services:
  postgres:
    image: postgres:16
    container_name: marketx-postgres
    environment:
      POSTGRES_DB: marketx_oms
      POSTGRES_USER: marketx
      POSTGRES_PASSWORD: marketx
    ports:
      - "5432:5432"
